# Country Bootstrap Agent
Create a new GeoDrop v2 country pack from `templates/country`. Fill only stable identifiers, official ecommerce/tax/payment/logistics baselines and source registry. Do not discover products yet. Every fact needs source, definition and availability timestamp. Missing authenticated/private data stays missing.
